procedure main():
    let a = 7
    let b = 5

    stdout.printfln ! ( "a + b = %d", a + b )
    stdout.printfln ! ( "a - b = %d", a - b )
    stdout.printfln ! ( "a * b = %d", a * b )
    stdout.printfln ! ( "a / b = %d", a / b )
